# Wildfire Evacuation Route & Risk Optimization

A Machine Learning project designed to identify high-risk communities and optimize evacuation routes during wildfire events in Los Angeles County. The system produces an actionable, data-driven vulnerability assessment across all 2,493 LA County census tracts.

**Course:** CPSC 483 — Machine Learning, CSU Fullerton  
**Author:** Hung (Quan) Truong

---

## Overview

This project combines geospatial analysis, social vulnerability data, and ML classification to:

1. **Assess community risk** — rank every LA County census tract by wildfire vulnerability using CDC Social Vulnerability Index (SVI) features and engineered spatial features (fire station proximity, hospital proximity, hydrant density, road density).
2. **Train a risk classifier** — Random Forest model that predicts low / moderate / high risk labels per tract.
3. **Optimize evacuation routes** — Dijkstra shortest-path routing on a filtered OSMnx road graph, with a penalty multiplier on high-risk road segments.
4. **Visualize results** — Interactive Folium choropleth maps and an optional Dash dashboard.

---

## Repository Structure

```
.
├── config.yaml                        # All paths, model hyperparameters, and settings
├── requirements.txt                   # Python dependencies (pip install -r requirements.txt)
│
├── data/
│   ├── raw/                           # Original, unmodified source files
│   │   ├── California_2020_SVI.csv           # CDC SVI 2020 (CA statewide; filtered to LA in notebook)
│   │   ├── LA_County_2020_Census_Tracts.geojson
│   │   ├── la_county.geojson                 # Neighborhood polygons for basemap & spatial joins
│   │   ├── Fire_Hydrants_(DWP).geojson       # LA DWP hydrant locations
│   │   └── Hospitals_and_Medical_Centers.geojson
│   │
│   ├── processed/                     # Cleaned and feature-enriched outputs
│   │   ├── svi_tracts.geojson                # 2,493 census tracts + 152 SVI columns
│   │   ├── la_county_svi.geojson             # 272 neighborhoods (population-weighted SVI)
│   │   ├── all_stations.geojson              # 412 fire stations (LAFD + LACoFD combined)
│   │   ├── lafd_stations.geojson             # 106 LAFD stations
│   │   ├── lafd_station_boundaries.geojson
│   │   ├── LACoFD_stations.geojson           # 174 LACoFD stations
│   │   ├── LACoFD_station_boundaries.geojson
│   │   ├── hospitals.geojson                 # 165 hospitals
│   │   ├── Fire_Hydrants.geojson             # 60,295 DWP hydrant points
│   │   ├── features.geojson                  # Final feature matrix (all tracts)
│   │   ├── tract_features_predicted.geojson  # Feature matrix + model predictions
│   │   └── escape_routes_roads.geojson       # Copy of drivable edges alongside processed layers
│   │
│   ├── external/                      # Large derived files (tracked via Git LFS)
│   │   ├── road_network.graphml              # OSMnx projected graph (EPSG:3310)
│   │   └── escape_routes_roads.geojson       # 61,375 drivable edges (motorway → tertiary)
│   │
│   └── cache/                         # OSMnx tile/graph download cache (auto-generated)
│
├── src/
│   └── data_processing/               # Data preparation notebooks (run once, in order)
│       ├── CensusTract_SVI.ipynb      # Joins CDC SVI to LA census tracts
│       ├── FireStations.ipynb         # Fetches LAFD & LACoFD stations from ArcGIS APIs
│       ├── FireHydrants.ipynb         # Cleans DWP hydrant dataset
│       ├── Hospital.ipynb             # Cleans hospital dataset
│       └── Escape_Route.ipynb         # Downloads OSMnx road network & extracts escape routes
│
├── notebooks/                         # Analysis & modeling notebooks (run in order)
│   ├── exploration.ipynb              # EDA — distributions, correlations, maps
│   ├── feature_engineering.ipynb      # Spatial joins → derived features (dist, density, etc.)
│   ├── model_trainning.ipynb          # Train / evaluate risk classifier; SHAP explainability
│   └── visualizations.ipynb          # Interactive Folium maps & Dash dashboard
│
└── outputs/
    ├── maps/
    │   ├── 01_risk_choropleth.html          # Risk score choropleth by census tract
    │   ├── 02_risk_with_infrastructure.html # Risk + fire stations, hospitals, hydrants overlay
    │   └── 03_evacuation_route.html         # Optimized evacuation routes
    └── models/
        ├── risk_classifier.pkl              # Trained Random Forest model
        └── scaler.pkl                       # Feature scaler
```

---

## Data Sources

| Dataset | Source | Format |
|---|---|---|
| CDC Social Vulnerability Index 2020 | CDC / ATSDR | CSV |
| LA County 2020 Census Tracts | LA County Open Data | GeoJSON |
| LA County Neighborhoods | LA County Open Data | GeoJSON |
| Fire Hydrants | LA DWP | GeoJSON |
| Hospitals & Medical Centers | LA County Open Data | GeoJSON |
| LAFD Fire Stations & Districts | LAFD ArcGIS REST API | Live fetch |
| LACoFD Fire Stations & Boundaries | LACoFD ArcGIS REST API | Live fetch |
| Road Network | OpenStreetMap via OSMnx | GraphML / GeoJSON |

---

## ML Pipeline

### Features

**SVI Composite & Themes (from CDC)**
- `rpl_themes` — overall social vulnerability percentile
- `rpl_theme_1` — socioeconomic status
- `rpl_theme_2` — household characteristics
- `rpl_theme_3` — racial & ethnic minority status
- `rpl_theme_4` — housing type & transportation

**Engineered Spatial Features**
- `dist_fire_station_m` — distance from tract centroid to nearest fire station
- `dist_hospital_m` — distance from tract centroid to nearest hospital
- `hydrant_count` — DWP hydrants within tract polygon
- `hydrant_density` — hydrant count / tract area (km²)
- `road_length_m` — total drivable road length clipped to tract
- `road_density` — road length / tract area (km²)
- `tract_area_km2` — tract area

### Risk Classifier

- **Model:** Random Forest (`n_estimators=200`, `max_depth=10`, `class_weight=balanced`)
- **Target:** 3-class risk label — Low [0–0.33), Moderate [0.33–0.66), High [0.66–1.0]
- **Validation:** 5-fold cross-validation, 80/20 train-test split
- **Explainability:** SHAP feature importance

### Evacuation Routing

- **Graph:** OSMnx drivable network filtered to motorway → tertiary roads (26,716 nodes, 61,375 edges), projected to EPSG:3310
- **Algorithm:** Dijkstra on `travel_time_s` edge weights
- **Risk penalty:** 5× weight multiplier applied to edges in high-risk tracts

---

## Setup & Usage

### 1. Install dependencies

```bash
# Python >= 3.10 recommended
pip install -r requirements.txt
```

### 2. Process raw data (run once)

Run notebooks in `src/data_processing/` in any order — each is independent:

```
CensusTract_SVI.ipynb → svi_tracts.geojson, la_county_svi.geojson
FireStations.ipynb    → all_stations.geojson, lafd_stations.geojson, LACoFD_stations.geojson
FireHydrants.ipynb    → Fire_Hydrants.geojson
Hospital.ipynb        → hospitals.geojson
Escape_Route.ipynb    → road_network.graphml, escape_routes_roads.geojson
```

### 3. Run the ML pipeline

Run notebooks in `notebooks/` in order:

```
1. exploration.ipynb        — understand the data
2. feature_engineering.ipynb — build features.geojson
3. model_trainning.ipynb    — train model, save risk_classifier.pkl
4. visualizations.ipynb     — generate maps in outputs/maps/
```

### 4. View outputs

Open any of the generated HTML maps directly in a browser:

```
outputs/maps/01_risk_choropleth.html
outputs/maps/02_risk_with_infrastructure.html
outputs/maps/03_evacuation_route.html
```

---

## Configuration

All paths, model hyperparameters, CRS settings, and routing parameters are centralized in [config.yaml](config.yaml). Edit that file to change model behavior without touching notebook code.

---

## License

See [LICENSE](LICENSE).
